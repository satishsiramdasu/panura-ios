# XcodeGen output (regenerated from project.yml)
*.xcodeproj
*.xcworkspace

# Xcode / SwiftPM
.build/
DerivedData/
*.xcuserstate
xcuserdata/
.swiftpm/

# macOS
.DS_Store

# Pasted CI build logs. One of these was committed once; never again.
temp.txt
*.buildlog

# Firebase config. This repo is public; CI writes the file from a secret.
Resources/GoogleService-Info.plist

# The screenshot run renders this onto the runner; it must never be committed.
Resources/screenshot-demo.mp4
